/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["plus.unsplash.com", "images.unsplash.com", "www.google.com", "upload.wikimedia.org"]
    }
}

module.exports = nextConfig
